<resources>

    <!-- Base application theme.
    Use this theme with no toolbar and white background-->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/app_background_white</item>
        <item name="colorControlNormal">@color/colorPrimary</item>
        <item name="android:windowBackground">@color/app_background_white</item>
        <item name="android:colorBackground">@color/app_background_white</item>
        <item name="actionBarTheme">@style/MyApp.ActionBarTheme</item>
    </style>
    <!--Use this theme with no toolbar and no status bar-->
    <style name="AppTheme.FullScreen" parent="AppTheme">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="MyApp.ActionBarTheme" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <!--back arrow color will be changed here-->
        <item name="colorControlNormal">@color/colorPrimary</item>
    </style>

    <style name="WhiteToolbar">
        <item name="colorControlNormal">@color/colorPrimary</item>
        <item name="colorPrimary">@color/app_background_white</item>
    </style>

    <!--Use this theme with toolbar and white background-->
    <style name="AppTheme.ToolBar.White" parent="AppTheme">
        <item name="colorAccent">@color/app_background_themed</item>
        <item name="android:colorBackground">@color/app_background_white</item>
    </style>

    <!--Use this theme with toolbar and themed background-->
    <style name="AppTheme.ToolBar.Themed" parent="AppTheme">
        <item name="colorAccent">@color/app_background_white</item>
        <item name="android:windowBackground">@drawable/img_themed_bg</item>
    </style>

    <!--Use this theme with no toolbar and image background-->
    <style name="AppTheme.Splash" parent="AppTheme.FullScreen">
        <item name="android:windowBackground">@drawable/img_splash_bg</item>
    </style>

    <style name="WeightWidth">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_weight">1</item>
    </style>

    <style name="WrapContent">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="MatchParent">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="MatchWidth">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>


    <style name="MatchHeight">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">match_parent</item>
    </style>

</resources>
